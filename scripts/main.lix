include "math.lix";
include "util.lix";

function multithreadable() {
    for (static i = 1; i <= 5; i++) {
        println("Hello from a different thread! (x" + i + ")");
    }
}

function test_async() {
    async(this::multithreadable);
    shutdown_execution_service();
}

function main() {
    f = factorial(5);
    println(f);
}
